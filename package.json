{
  "name": "mosa",
  "private": true,
  "description": "High-performance image processing engine with Leica camera style simulation",
  "license": "Apache-2.0",
  "packageManager": "pnpm@10.32.1",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter mosa-desktop dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "clean": "turbo clean",
    "format": "prettier --write \"apps/**/*.{ts,tsx,less,json}\" \"packages/**/*.{ts,tsx,json}\" \"tooling/**/*.{js,json}\""
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "electron",
      "esbuild",
      "sharp",
      "lightdrift-libraw"
    ]
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "turbo": "^2.5.0"
  }
}
